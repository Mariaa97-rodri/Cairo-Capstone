# ?????????????????????????????????????????
# Cairo Backend ? Application Configuration
# ?????????????????????????????????????????

# Server
server.port=8081

# Database ? update username/password to match your local MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/cairo_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=buvwex-nyFdep-vinve7
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT ? change the secret to any long random string (min 256 bits)
app.jwt.secret=cairo-super-secret-key-change-this-in-production-min-256-bits
app.jwt.expiration-ms=86400000

# Claude AI ? store your real key in environment variable, not here
app.claude.api-key=${CLAUDE_API_KEY:your-claude-key-here}
app.claude.model=claude-sonnet-4-20250514
app.claude.base-url=https://api.anthropic.com

# Logging
logging.level.com.cairo=DEBUG
logging.level.org.springframework.security=DEBUG
